<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login | URL Shortener</title>
    <link rel="stylesheet" href="login.css" />
  </head>
  <style>
    /* 🌈 Base Styles */
    body {
      margin: 0;
      font-family: "Poppins", sans-serif;
      background: linear-gradient(135deg, #0a1128, #001f54, #034078);
      color: #fff;
      overflow: hidden;
      height: 100vh;
    }

    /* ⚡ Layout */
    .login-container {
      display: flex;
      /**change the adjustment of the form*/
      flex-direction: row-reverse;
      height: 100vh;
    }

    /* 🧊 Glassmorphic Form */
    .login-form {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 60px 80px;
      background: rgba(255, 255, 255, 0.08);
      backdrop-filter: blur(18px);
      border-right: 1px solid rgba(255, 255, 255, 0.15);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    }

    .login-form h2 {
      font-size: 2.3rem;
      margin-bottom: 10px;
    }

    .subtitle {
      color: #cfd8e3;
      margin-bottom: 40px;
    }

    /* 🧩 Input Fields */
    .input-group {
      display: flex;
      flex-direction: column;
      margin-bottom: 25px;
    }

    .input-group label {
      font-size: 0.9rem;
      color: #dce4f0;
      margin-bottom: 8px;
    }

    .input-group input {
      padding: 12px 15px;
      border-radius: 30px;
      border: none;
      outline: none;
      font-size: 1rem;
      color: #333;
    }

    /* 🔗 Options & Buttons */
    .options {
      text-align: right;
      margin-bottom: 25px;
    }

    .forgot {
      text-decoration: none;
      color: #00d4ff;
      font-size: 0.9rem;
    }

    .forgot:hover {
      text-decoration: underline;
    }

    .btn {
      background: linear-gradient(135deg, #00d4ff, #0078ff);
      border: none;
      padding: 12px 30px;
      border-radius: 30px;
      color: white;
      font-weight: 600;
      font-size: 1rem;
      cursor: pointer;
      transition: 0.3s;
      width: 100%;
    }
    .btn:hover {
      transform: scale(1.05);
    }

    .register-text {
      text-align: center;
      margin-top: 25px;
      color: #cfd8e3;
    }
    .register-text a {
      color: #00d4ff;
      text-decoration: none;
    }
    .register-text a:hover {
      text-decoration: underline;
    }

    /* 🖼️ Right Image Section */
    .login-image {
      flex: 1.2;
      background: url("https://images.unsplash.com/photo-1522202176988-66273c2fd55f?auto=format&fit=crop&w=1920&q=80")
        no-repeat center center / cover;
      position: relative;
    }

    .login-image::after {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(10, 15, 35, 0.45);
      backdrop-filter: blur(5px);
    }

    .overlay-text {
      position: absolute;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      z-index: 2;
      max-width: 400px;
    }

    .overlay-text h1 {
      font-size: 3rem;
      margin-bottom: 10px;
      color: #ffffff;
    }

    .overlay-text p {
      color: #cfd8e3;
      font-size: 1.2rem;
    }

    /* 📱 Responsive Design */
    @media (max-width: 900px) {
      .login-container {
        flex-direction: column;
      }
      .login-form,
      .login-image {
        flex: none;
        width: 100%;
        border: none;
      }
      .login-form {
        padding: 40px 30px;
        border-bottom: 1px solid rgba(255, 255, 255, 0.15);
      }
      .overlay-text {
        bottom: 30px;
        left: 30px;
      }
      .overlay-text h1 {
        font-size: 1.8rem;
      }
    }
  </style>
  <body>
    <div class="login-container">
      <!-- Left Glass Form -->
      <div class="login-form">
        <h2>Welcome Back 👋</h2>
        <p class="subtitle">Login to continue your journey</p>

        <form>
          <div class="input-group">
            <label>Email Address</label>
            <input type="email" placeholder="Enter your email" required />
          </div>

          <div class="input-group">
            <label>Password</label>
            <input type="password" placeholder="Enter your password" required />
          </div>

          <div class="options">
            <a href="#" class="forgot">Forgot Password?</a>
          </div>

          <button type="submit" class="btn">Login</button>

          <p class="register-text">New User? <a href="#">Register here</a></p>
        </form>
      </div>

      <!-- Right Image Side -->
      <div class="login-image">
        <div class="overlay-text">
          <h1>Shorten, Share & Simplify</h1>
          <p>Turn your long URLs into sleek, trackable links in seconds.</p>
        </div>
      </div>
    </div>
  </body>
</html>
